import * as React from 'react';
import { View, Text } from 'react-native';

interface Props {}

const Home = ({}: Props) => {
  return (
    <View>
      <Text>HomePage</Text>
    </View>
  );
};

export default Home;
